name: Build and Deploy Application

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build project
        run: mvn clean package -DskipTests

      - name: Debug - Show built files
        run: ls -lah target/

      # Copiar JAR para EC2
      - name: Copy JAR to EC2
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          source: "target/*.jar"
          target: "/home/ec2-user/event-api/"
          strip_components: 1
          timeout: 60s
          command_timeout: 10m

      # Deploy e restart
      - name: Deploy and Restart Service
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          timeout: 60s
          command_timeout: 10m
          script: |
            echo "✅ Conexão SSH estabelecida com sucesso!"
            echo ""
            echo "📍 Informações do servidor:"
            echo "Hostname: $(hostname)"
            echo "User: $(whoami)"
            echo "OS: $(cat /etc/os-release | grep PRETTY_NAME)"
            echo "Java: $(java -version 2>&1 | head -n 1)"
            echo ""
            echo "📂 Estrutura de diretórios:"
            ls -lah /home/ec2-user/event-api/ 2>/dev/null || echo "Diretório event-api não existe ainda"
            echo ""
            echo "🔧 Status do serviço:"
            sudo systemctl status event-api --no-pager || echo "Serviço ainda não configurado"